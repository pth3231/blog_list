# Getting started with REST Client

Within the directory `rest/`, there are files that are written in `*.rest` extension. It is a quick and convenient way to test RESTful API using REST Client extension in Visual Studio Code.

To install, find `REST Client` extension in the `Extension` option, or Marketplace.